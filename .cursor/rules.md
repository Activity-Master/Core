# Cursor Workspace Rules
- Load `RULES.md`, Document Modularity, and Forward-Only policies before generating docs or code.
- Sequence work through Stage 1 → Stage 2 → Stage 3 → Stage 4; blanket approvals allow automatic progression but still mention when stages are auto-advanced.
- Reference `rules/` submodule as the source of truth for stack-specific guidance (GuicedEE, Hibernate Reactive, PostgreSQL, etc.).
- When prompted for stage approval, present the standardized options block (Stages must echo `Options:` block exactly as documented).
